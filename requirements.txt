# Watsu Gift Voucher System — V1 Dependencies
# Python 3.12
# Versions PINNED to match production (vouchers.soulwaves.org) as of 2026-06-09.
# Reason: floating >= pins pulled Starlette 1.x, which removed the old
# TemplateResponse(name, context) signature and 500'd every page. Production
# runs Starlette 0.52.x where the existing code works unchanged.

# Web framework
fastapi==0.135.1
uvicorn[standard]==0.42.0
# Starlette pinned explicitly — this is the package whose 1.x release broke rendering.
starlette==0.52.1

# Database
sqlalchemy==2.0.48

# Templates
jinja2==3.1.6

# Form data parsing (required by FastAPI for HTML forms)
python-multipart==0.0.22

# PDF generation (reportlab directly — xhtml2pdf can't render Hebrew)
reportlab==4.4.10

# RTL/bidi text support for Hebrew in PDF
python-bidi==0.6.7

# QR code generation
qrcode==8.2

# Image handling
Pillow==12.1.1

# QR encryption
cryptography==46.0.5

# Static file serving
aiofiles==25.1.0

# Authentication — WebAuthn/FIDO2 biometric auth
webauthn==2.7.1
