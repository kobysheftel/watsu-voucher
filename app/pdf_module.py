# PDF module — fully implemented in Step 6
from pathlib import Path

def generate_pdf(voucher, folder: Path) -> Path:
    """Placeholder — returns dummy path until Step 6."""
    return folder / "voucher.pdf"
