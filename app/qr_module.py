# QR module — fully implemented in Step 7
from pathlib import Path

def generate_qr(voucher, folder: Path) -> Path:
    """Placeholder — returns dummy path until Step 7."""
    return folder / "qr.png"
